val : 変更禁止
var : 変更可能
++,-- : インクリメント、デクリメントはScalaでサポートしていない
Javaの標準機能 : ""や結合、langthなどは一通りサポートしている
 配列 : []でアクセスする事は出来ない。(数)でアクセス可能
forループ : for(初期; 条件; 再帰時)の記述は出来ない
	for(データ <= 配列)　の記述になる
	for(i <- 初期値 to 終了値)　で数字をループする事が可能
	for(i <- 初期値 to 終了値 by 加算値) の記述もある
foreach : 配列系要素の標準処理
	C#のデリゲート関係のループと基本同じ
	いろいろな記述式があるので、細かいやり方は別のタイミングで
